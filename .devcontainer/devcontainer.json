{
  "name": "py-analysis",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  // "runArgs": [
  //   "--rm"
  // ],
  "runArgs": ["--name=my-analytics"],
  // "workspaceFolder": "/app",
  "mounts": [
    "source=${localWorkspaceFolder}/src,target=/app,type=bind,consistency=cached"
  ]
}

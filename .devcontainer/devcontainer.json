{
  "name": "exploration",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile.dx"
    // "dockerComposeFile": "docker-compose.yml"
  },
   // "runArgs": [
  //   "--rm"
  // ],
  // "runArgs": ["--name=exploration"],
  "workspaceFolder": "/app",
  "mounts": [
    "source=${localWorkspaceFolder},target=/app,type=bind,consistency=cached"
  ]
}

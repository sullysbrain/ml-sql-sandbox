[project]
name = "analysis-stack"
version = "0.1.0"

[project.dependencies]
pandas = "^2.2"
scikit-learn = "^1.5"
torch = { version = "^2.3", extras = ["cpu"] }
jupyterlab = "^4.2"

[tool.uv.lock]   # generated by `uv lock` – commit the uv.lock file too
